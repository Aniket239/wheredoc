<!DOCTYPE html>
<html>
  <head>
    <title>wheredoc</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if !(controller_name == 'setup' && (action_name == 'login' || action_name == 'signup' || action_name=='employees' || action_name=='index' && !logged_in?)) %>
    <%= render 'layouts/navbar' %>
  <% end %>
  <%= yield %>
  </body>
</html>
